name: Daily Scholarship Hunt

on:
  schedule:
    # Runs at 06:00 UTC (Universal Time) every day
    - cron: '0 6 * * *'
  workflow_dispatch: # Allows you to click a button to run it manually anytime

jobs:
  hunt-and-process:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        # We install all the libraries your scripts need
        run: |
          pip install google-api-python-client supabase requests python-dotenv beautifulsoup4 fake-useragent pypdf google-generativeai

      - name: Run Hunter (Find new URLs)
        env:
          # We pass the secrets from GitHub to the script
          SUPABASE_URL: ${{ https://iapvwvpoqmadrxzhqggj.supabase.co }}
          SUPABASE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlhcHZ3dnBvcW1hZHJ4emhxZ2dqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjUyMDg2MzMsImV4cCI6MjA4MDc4NDYzM30.sLsLBDbH7no-bGI-wes5GRzs_w03vOVP-IkkJJz27QU }}
          GOOGLE_API_KEY: ${{ AIzaSyByqutLK14aVfNVY55QcEVTxZ4NuCuPbsU }}
          SEARCH_ENGINE_ID: ${{ c66dcaea9f54f4e6b }}
        run: python scholarship_hunter.py

      - name: Run Scraper (Read Text/PDFs)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python scholarship_scraper.py

      - name: Run Embedder (Memorize with AI)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scholarship_embedder_gemini.py
